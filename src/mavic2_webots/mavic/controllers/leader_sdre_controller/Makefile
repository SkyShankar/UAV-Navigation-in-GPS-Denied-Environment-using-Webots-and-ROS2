# Minimal Makefile for Python controller
WEBOTS_HOME = $(shell webots --version --path)
CONTROLLER_NAME = ros2_sensor_publisher
CFLAGS = -O2 -Wall
CXXFLAGS = $(CFLAGS)

all:
	echo "Python controller - nothing to compile"

clean:
	echo "Clean complete"
